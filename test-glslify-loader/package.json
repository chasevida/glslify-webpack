{
  "name": "test-glslify-loader",
  "version": "0.1.0",
  "description": "A glslify-loader example",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clear": "rm -rf ./dist",
    "start:file": "yarn clear && webpack src/glsl-file.js && serve ./dist",
    "start:import": "yarn clear && webpack src/glsl-import.js && serve ./dist",
    "start:inline": "yarn clear && webpack src/glsl-inline.js && serve ./dist"
  },
  "dependencies": {
    "glsl-noise": "^0.0.0",
    "glslify": "^6.1.0"
  },
  "devDependencies": {
    "glslify-loader": "^1.0.2",
    "html-webpack-plugin": "^2.30.1",
    "raw-loader": "^0.5.1",
    "serve": "^6.4.3",
    "webpack": "^3.10.0"
  }
}
